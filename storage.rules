rules_version = '2';
service firebase.storage {
  // Same trust model as firestore.rules — namespaced by VITE_FAMILY_ID,
  // not real auth. See the comment there for the tradeoff.
  match /b/{bucket}/o {
    match /families/{familyId}/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
